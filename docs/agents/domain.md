# Domain Docs

This repository has a single domain context.

Before exploring or changing a feature, read the root `CONTEXT.md` and the
relevant decisions in `docs/adr/`. Use the glossary's canonical language in
issues, specifications, tests, and implementation discussions. If a proposed
change conflicts with an ADR, surface that conflict explicitly.
